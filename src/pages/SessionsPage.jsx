const SessionsPage = () => {
  return (
    <div>
      <h1 style={{ fontSize: '20px', fontWeight: 700, marginBottom: '4px' }}>Sessions & Logs Browser</h1>
      <p style={{ fontSize: '12px', color: 'var(--text3)', fontFamily: 'var(--mono)', marginBottom: '20px' }}>// click any session to replay terminal commands</p>
      {/* TODO: Implement Table and Terminal Replay */}
    </div>
  );
};

export default SessionsPage;
